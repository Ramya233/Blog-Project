import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)

// Questions
// 1) What is react router dom ? 
// 2) What is an outlet ? 
// 3) What is all the BrowserRouter / Routes / Route 
// 4) How to use Tailwind Css ? and how to use it ?
// 5) What is the use of <Links> <Links /> -> in React-Router ?